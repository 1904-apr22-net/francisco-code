git init
git clone
git log
git push
git add <path>
    this wlill stage all local changes
    to any files/folders at/under that path.
    usually: "git add ." from the top-level repo folder
    (because "." in bash means, the current directory)